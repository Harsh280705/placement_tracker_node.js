{
  "name": "placement-tracker-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Placement Tracker API — Node.js + Express + PostgreSQL (uses nodejs_applications table)",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "init-db": "node scripts/init-db.js",
    "test": "node --test tests/validation.test.js tests/applications.test.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "supertest": "^7.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
